name: Deploy PR previews
on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - closed

concurrency: preview-${{ github.ref }}

env:
  DEPLOY_PATH: tmp
  NODE_VERSION: "22.16.0"

jobs:
  deploy-preview:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create a folder with all compiled apps and pkgs
        if: github.event.action != 'closed'
        run: |
          mkdir -p ${{ env.DEPLOY_PATH }}
        

      - name: Install and Build
        if: github.event.action != 'closed'
        run: ./.github/scripts/build-vite-app.sh
        env:
          PACKAGE_PATH: apps/LimesUI
          TARGET_FOLDER: LimesUI
        continue-on-error: true
   
      - name: Deploy preview
        if: github.event.action != 'closed'
        run: |
          echo "<li><a href='./LimesUI/index.html' target="_blank">LimesUI" >> ${{ env.DEPLOY_PATH }}/index.html
        
      - uses: rossjrw/pr-preview-action@v1
        with:
          source-dir: ${{ env.DEPLOY_PATH }}/
